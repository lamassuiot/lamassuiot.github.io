import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
    cardActionArea: {
        textAlign: "center",
        width: "100%",
        height: "100%"
    },
    card: {
        textAlign: "center",
        width: "100%",
        height: "100%"
    },
    typoCardTitle: {
        fontWeight: "bold"
    }
    
}));