import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
    gridItem: {
        textAlign: "center",
    },
    typoSectionTitle: {
        fontWeight: "bold"
    }
    
}));